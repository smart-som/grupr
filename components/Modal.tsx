import React, { useState } from "react";

function Modal() {
  return (
    <section className="fixed inset-0 backdrop:blur-md flex justify-center p-3 bg-white/60 items-center">
      Modal
    </section>
  );
}

export default Modal;
